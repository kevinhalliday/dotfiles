#!/bin/bash
# apply "$HOME/lib/alacritty-theme/themes/tomorrow_night_bright.yaml"

alacritty-colorscheme \
  -c $HOME/.alacritty.yml \
  apply "$HOME/lib/alacritty-theme/themes/tomorrow_night_bright.yaml"
